version: 2

sources:
  - name: raw
    description: "Raw market data loaded by the Python ETL pipeline."

    schema: main

    tables:
      - name: market_prices
        identifier: raw_market_prices
        description: "Daily market prices for the investment instruments tracked by the pipeline."